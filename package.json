{
  "name": "maakabhosadaaag",
  "displayName": "maakabhosadaaag",
  "description": "Plays a sound when your terminal command fails. Emotional damage for your shell.",
  "version": "0.0.1",
  "publisher": "Daijoubu",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/shubhamraghav/errorsoundextention"
  },
  "engines": {
    "vscode": "^1.93.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "terminal",
    "sound",
    "error",
    "fun",
    "productivity",
    "audio",
    "notification"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "maakabhosadaaag",
      "properties": {
        "maakabhosadaaag.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable or disable the error sound. Turn it off if you can't handle the truth."
        },
        "maakabhosadaaag.volume": {
          "type": "number",
          "default": 0.5,
          "minimum": 0,
          "maximum": 1,
          "description": "Volume level from 0 (silent regret) to 1 (full disappointment). macOS only."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.93.0",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "@vscode/vsce": "^3.7.1"
  }
}
